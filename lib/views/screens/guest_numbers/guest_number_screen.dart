import 'package:flutter/widgets.dart';

class GuestNumberScreen extends StatelessWidget {
  const GuestNumberScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}